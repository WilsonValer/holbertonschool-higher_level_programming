#!/usr/bin/python3
def multiple_returns(sentence):
    if not sentence:
        length = len(sentence)
        firt = sentence[0]
        return length, firt
    else:
        length = len(sentence)
        firt = sentence[0]
        return length, firt
